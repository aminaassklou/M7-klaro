# Copiez ce fichier en .env (jamais commite, voir .gitignore) et renseignez
# votre cle. Recuperez-la sur https://console.mistral.ai/ (une cle par binome
# suffit). config.py la charge automatiquement au demarrage via python-dotenv.
MISTRAL_API_KEY=
